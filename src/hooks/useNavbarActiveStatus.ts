import React from "react";
import { useLocation } from "react-router";

function useNavbarActiveStatus() {
  const location = useLocation();
  // const activeStatus = location.pathname + location.search ==
  return "";
}

export default useNavbarActiveStatus;
